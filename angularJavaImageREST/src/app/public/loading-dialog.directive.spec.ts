import { LoadingDialogDirective } from './loading-dialog.directive';

describe('LoadingDialogDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadingDialogDirective();
    expect(directive).toBeTruthy();
  });
});
