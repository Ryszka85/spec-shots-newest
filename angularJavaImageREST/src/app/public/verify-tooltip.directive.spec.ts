import { VerifyTooltipDirective } from './verify-tooltip.directive';

describe('VerifyTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new VerifyTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
